Step 14 Notes: 

Goal - Fix the 'blink' issue



1. Add 'Items' factory service in item.js for getAll items and 'newItem'

2. Add another service 'getItems' to create all items array and other functions to create and update items
	and to update the item array

3. Add Items, getItems, Auth & User service dependencies to browse controller. Auth is needed to get logged in user info from service

4. Add code to get user info in browse controller

5. Replace $http call in browse controller to an array call to fix 'blink' issue

