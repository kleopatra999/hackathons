Step 7:

In this step we show how a simple angular js ng-repeat loop works from a json array 

directly assigned to $scope for simplicity. We can even break it up so the json data is in a flat file.

Also, the .css is build as you proceed through the demonstration to show how a 'class' item can be used to modify display. 

MILESTONE ALERT - Let's deploy our Node JS Application using Angular to the cloud!

Additional - Look up some more bootstrap classes