{
  "name": "sampleApp",
  "version": "0.0.1",
  "dependencies": {
    "body-parser": "1.12.4",
    "express": "4.12.4"
  },
  "main": "server.js",
  "scripts":{ 
    "start": "node server.js",
    "preinstall": "rm -f -R node_modules workshop-dist.zip",
    "prepublish": "zip -r workshop-dist.zip manifest.json *.js *.html node_modules scripts"
  },
  "engines": {
    "node": ">=0.10.0"
  }
}
